{
  "$id": "https://schema.steadybit.io/discovery-kit/indexResponse",
  "title": "Discovery Extension Index Response",
  "description": "Lists all discoveries, target types and target attributes that the platform/agent should handle.",
  "type": "object",
  "properties": {
    "discoveries": {
      "type": "array",
      "items": {
        "$ref": "describingEndpointRef"
      },
      "uniqueItems": true
    },
    "targetTypes": {
      "type": "array",
      "items": {
        "$ref": "describingEndpointRef"
      },
      "uniqueItems": true
    },
    "targetAttributes": {
      "type": "array",
      "items": {
        "$ref": "describingEndpointRef"
      },
      "uniqueItems": true
    }
  }
}